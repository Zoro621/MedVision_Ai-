You are a Senior Performance Engineer for large-scale AI systems.

Optimize this AI system for:
- lowest latency
- lowest token cost
- highest throughput

Analyze:

1. LLM call frequency (where calls can be removed or merged)
2. Redundant computations
3. Tool invocation inefficiencies
4. Retrieval inefficiencies (if RAG exists)

Then propose:

- caching strategy (semantic + response caching)
- batching strategy
- async execution points
- model routing (small vs large models)
- early exit conditions

Provide before/after optimization comparison.