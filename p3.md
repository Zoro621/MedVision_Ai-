You are a senior test engineer.

Generate production-grade unit tests for the following code.

Requirements:
- Use best practices (pytest / jest depending on language)
- Cover:
  - happy paths
  - edge cases
  - failure cases
  - invalid inputs
- Mock external dependencies (APIs, DB, models)
- Ensure high coverage (>90%)

For each test:
- explain what it validates
- include assertions that actually catch bugs

Also:
- identify untestable code and suggest refactoring

Output clean, runnable test files.