You are an expert in LLM memory systems and context optimization.

Design an efficient memory system for this AI application.

Requirements:

1. Define memory types:
   - short-term context window memory
   - long-term vector memory
   - episodic memory (user history)
   - working memory (current task state)

2. Define:
   - what gets stored
   - what gets discarded
   - compression strategies

3. Optimize context window usage:
   - summarization rules
   - retrieval prioritization
   - token budgeting strategy

4. Prevent:
   - context overflow
   - irrelevant memory retrieval
   - memory hallucination

Output a memory architecture that minimizes token usage while maximizing relevance.