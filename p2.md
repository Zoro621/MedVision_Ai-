Act as a malicious QA tester trying to break this application.

Your goal is to find ALL possible edge cases and failure points.

Test categories:
1. Input edge cases:
   - empty, null, undefined
   - extremely large inputs
   - invalid formats (wrong JSON, wrong types)
   - special characters / injection attempts

2. UI edge cases:
   - rapid clicks
   - double submissions
   - navigation during loading
   - stale state issues

3. Backend edge cases:
   - API timeout
   - partial responses
   - corrupted data
   - concurrent requests

4. AI/ML features (if present):
   - hallucination-prone prompts
   - adversarial inputs
   - long context overflow
   - inconsistent outputs

5. State management:
   - reload during operations
   - session expiry
   - inconsistent cache

Output:
- A structured list of edge cases
- Which ones will break the system
- Why they break it
- Suggested fixes

Think like an attacker AND a frustrated user.